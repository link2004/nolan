# 判断ログ

## 2026-07-05 Storyboard → 撮影ワークフロー統合

- **撮影対象ライン**: 全ライン(参照メディアはインスピレーション扱い)。「空のラインのみ」「撮影前に選択」は不採用 — ユーザー回答(ADR-004)

## 2026-07-05 スクリプト付き撮影フロー

- **撮影対象**: 動画のみ(写真は作らない)。AIディレクター構想は継続で、まず土台を作る — ユーザー回答
- **コアフロー**: 横持ち / 画面下にスクリプト表示 / 録画→プレビュー→OKで次のスクリプト — ユーザー指定
- **作業場所**: 別セッションが CueCam を Ten-K Vault クライアントにピボット中と判明。カメラは「同じプロジェクトの別機能として」実装 — ユーザー回答。衝突回避のため機能コードは正規3層に置きつつ、起動は追加ターゲット ShootCam から行う構成を採用(ADR-003)
